import React from "react"
import styled from "styled-components"

const ContactMeWrapper = styled.div`
    height: calc(100vh - 95px);
`

function ContactMe() {
    return <ContactMeWrapper id="contact">ContactMe</ContactMeWrapper>
}

export default ContactMe
