const initalState = {}

export const githubReducer = (state = initalState, action) => {
    switch (action.type) {
        case "":
            return {}
        default:
            return state
    }
}
